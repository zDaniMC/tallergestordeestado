name: tallergestordeestado
description: A new Flutter project.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # === Dependencias necesarias ===
  intl: ^0.19.0
  sqflite: ^2.3.0
  path: ^1.9.0
  connectivity_plus: ^6.0.3
  uuid: ^4.3.3
  http: ^1.2.1


  flutter_riverpod: ^2.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
